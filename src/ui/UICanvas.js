import {
  GAME_WIDTH,
  GAME_HEIGHT,
  colors
} from "../services/services";


export default class UICanvas {
  constructor(game) {
    //this.game = game;

    this.opacity = 1.0;
    this.shadowColor = "transparent";
    this.shadowBlur = 0;
    }

}
